let numbersTextArea = document.querySelector("textarea") as HTMLTextAreaElement;
let analyzeButton = document.querySelector("button") as HTMLButtonElement;

analyzeButton.addEventListener("click", analyzeNumbers);

function analyzeNumbers(){
    //TODO
}
